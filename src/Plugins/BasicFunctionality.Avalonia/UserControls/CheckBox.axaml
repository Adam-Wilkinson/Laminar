<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:uid="using:Laminar.PluginFramework.UserInterface.UserInterfaceDefinitions"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="40" d:DataContext="{x:Static uid:Checkbox.DesignInstance}"
             x:DataType="uid:Checkbox+UITarget"
             x:Class="BasicFunctionality.Avalonia.UserControls.CheckBox">
	<Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Name="MainGrid">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" SharedSizeGroup="SG_PreTextMargin"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<TextBlock Grid.Column="1" Name="NameBlock" Text="{Binding Name}" VerticalAlignment="Center"/>
		<CheckBox Grid.Column="2" HorizontalAlignment="Center" IsChecked="{Binding Value}" Margin="5,0" VerticalAlignment="Center"/>
	</Grid>
</UserControl>
